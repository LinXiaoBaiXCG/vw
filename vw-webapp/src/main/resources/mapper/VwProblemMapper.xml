<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.linxiaobaixcg.repository.VwProblemRepository">

    <sql id="table">
        vw_problem o
    </sql>

    <sql id="column">
        o.id as id,
        o.user_id as userId,
        o.title as title,
        o.vw_describe as vwDescribe ,
        o.status as status,
        o.create_time as createTime,
        o.update_time as updateTime
    </sql>

    <select id="getListByTitle" resultType="io.github.linxiaobaixcg.entity.VwProblem">
        select
        <include refid="column"/>
        from
        <include refid="table"/>
        where
        o.status = 1
        and o.title like concat(concat('%',#{title,jdbcType=VARCHAR} ),'%')
        order by
        o.create_time desc
    </select>
</mapper>

